# Gulp boilerplate
- Sass
- ES6 (Babel)
- Uglidy JavaScript
- Browsersync
- Minify css
- Delete not used css rules

# Set up

## Requirements 

- Gulp `npm install gulp -g`

## Instalation 

- `yarn install`
- `gulp` for development
- `gulp build` for distribution
